plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = 'expo.modules.moekoenative'
version = '1.0.0'

android {
  namespace "expo.modules.moekoenative"
  defaultConfig {
    versionCode 1
    versionName "1.0.0"
  }
}

dependencies {
  implementation "androidx.media:media:1.7.0"
  implementation "androidx.palette:palette:1.0.0"
  // 液态玻璃已改为零依赖自包含实现（见 LiquidGlassSurfaceView.kt）：
  // 通过 bindBackdrop 拿到背后内容视图，将其绘制到离屏缓冲后做模糊再贴回，真正「看得见背后被模糊的画面」；
  // API 31+ 走 RenderNode + RenderEffect.createBlurEffect 的 GPU 硬件模糊，
  // API<31 走「降采样 Bitmap -> 3 趟盒式模糊」降级（放大即柔化）。
  // 不再依赖任何第三方 JitPack / Maven 模糊库，避免包名不匹配与仓库不可达导致的编译失败。
}
